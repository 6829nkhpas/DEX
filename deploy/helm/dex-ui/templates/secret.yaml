{{- $secretName := .Values.secrets.secretName -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "dex-ui.labels" . | nindent 4 }}
type: Opaque
data: {}
  # Populate via kubectl or external secrets operator:
  #   kubectl create secret generic {{ $secretName }} \
  #     --from-literal=VITE_WS_TOKEN=<token> \
  #     --from-literal=VITE_AUTH_TOKEN=<token>
