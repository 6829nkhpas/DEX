apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dex-ui.fullname" . }}-config
  labels:
    {{- include "dex-ui.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
