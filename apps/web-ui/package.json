{
  "name": "@dex/web-ui",
  "version": "0.1.0",
  "private": true,
  "description": "DEX Web UI — API and WebSocket client layers",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
