[package]
name = "matching-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
types = { path = "../../libs/types" }
rust_decimal = "1.33"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[dev-dependencies]
proptest = "1.4"
criterion = "0.5"
